TransOrg Analytics

1. What is React ?
2. Why React ?
4. How React makes sure to update specific part of DOM instead of complete DOM updation ?
5. Which Data Structure React Uses ?
6. What is Server Components ? What was the recent issue with that ?
7. CSR vs SSR
8. What is Tree Shaking ? Have you implemented it ?
9. What is Web Worker ? Example of Web Worker ?
10. Different ways to optimize the performance.
11. What is Authentication ? JWT question ? Authorization ? Protected Route ? Role Based Auth ?
12. What is React Fiber ? It's implementation usage ?
13. How to implement Infinite Scroll in vanilla JS ?
14. What is Code Splitting ? How to implement code splitting ? Real Life Example of it ?
15. Code Splitting using React Router DOM ?


Vernost


EXL
1. What is React ?
2. Why React ?
3. Advantages of React ?
4. What is Custom Hooks ? Code for custom debounce hook
5. Timer task with 4 buttons -> Start, Stop, Reverse, Reset
6. State vs Props ?
7. What is Prop Drilling ? How to avoid it ?
8. Difference between Redux and Context API ? When to use them with scenario ?
9. What is Hooks ?
10. Is Javascript synchronous language ? How does it handle asynchronous task ?
11. What is Closure ? With Example ? Advantages and Disadvantages of using it.
12. What is Currying ? Why Currying ? Benefits of Currying
13. var vs let vs const
14. Hoisting
15. Shadowing ? Legal and Illegal shadowing ?
16. What is Promises ? Difference between Promise and async await ?
17. Difference between Promise.all, Promise.allSettled, Promise.race, Promise.any
18. Which maintains the order of execution between Promise.all and Promise.allSettled
19. What is Typescript ? Benefit of using Typescript ?
20. Difference between Interface and Types ?
21. Difference between ENUM and Union ?
22. Difference between any and unknown ?
23. What is Tuple ?
24. What is Generic Type ?


Cognizant
1. Debounce
2. Counter with even increment, odd increment
3. useMemo vs useCallback
4. Re-rendering
5. Context API vs Redux
6. React.memo example
7. type Props = { user?: { name: string } };
    export function Header({ user }: Props) {
        return <h1>{user!.name}</h1>; 
    }
8. export default function App() {
    const onChange = (e: any) => console.log(e.target.value)
        return (
            <input onChange={onChange} />
        )
    }
9. export default function App({ setUser }: { setUser: (n: number) => void }) {
        setUser(1)
        return (
            <div></div>
        )
    }
10. Status codes
11. Benefit of using React JS
12. What is Virtual DOM


Capgemini
1. Data Analytics ? User activity ?
2. What is React ? Benefit of React ? Why React ?
3. Performance Optimization Techniques
4. E2E request flow library (data dog) 
5. React vs Next
6. useMemo vs useCallback
7. Context API vs Redux
8. Security
9. Some management questions
10. CI/CD tools
11. Internal/External storages like Firebase
12. Cloud Services


Infogain
1. Hoisting, error types, reassignment to const and how reassignment in setState works
2. Shallow, Deep copy
3. Promise, different methods of Promise, fulfilled vs settled, different promise states
4. Execution flow of JS, event loop
5. Different ways of optimizing React app
6. useMemo vs useCallback
7. Virtual DOM
8. React Fibre
9. Transitive Dependency
10. Reconciliation, Diffing, if the tree is large then how diffing algo works in the efficient manner ?
11. Redux, flow of redux, middleware, explain button clicked flow in redux
12. Redux vs RTK, why RTK was introduced, what is proxy while consoling state in the reducer, state mutability
13. impure vs pure function
14. build tool, transpiler, react element, custom -> react element or JSX to JS
15. guess the output
console.log('start');
const promise1 = Promise.resolve().then(() => {
  console.log('promise1');
  const timer2 = setTimeout(() => {
    console.log('timer2')
  }, 0)
});
const timer1 = setTimeout(() => {
  console.log('timer1')
  const promise2 = Promise.resolve().then(() => {
    console.log('promise2')
  })
}, 0)
console.log('end')
16. Task
    a. Render 4 cards (initially and then make it dynamic), mark some kind of identification to track whether the specific card is clicked or not
    b. Show text message like which card is clicked
    c. Once all the cards are clicked then show :- All cards showed
    d. Add reset button to reset the stats



1. Stateful, Stateless APIs
2. Async, Sync APIs
3. WebSockets
4. REST, SOAP
5. Security related questions
6. Callback is Sync or Async
7. Generate form from this :-
  const formConfig = [
    {
      name: 'country',
      label: 'Country',
      type: 'select',
      options: [
        { label: 'India', value: 'india' },
        { label: 'USA', value: 'usa' },
        { label: 'Other', value: 'other' },
      ],
      validation: { required: true },
    },
    {
      name: 'customCountry',
      label: 'Enter Country Name',
      type: 'text',
      dependsOn: 'country',
      showIf: 'other',
    },
    {
      name: 'state',
      label: 'State (India)',
      type: 'select',
      dependsOn: 'country',
      showIf: 'india',
      options: [
        { label: 'Maharashtra', value: 'maharashtra' },
        { label: 'Rajasthan', value: 'rajasthan' },
        { label: 'Gujarat', value: 'gujarat' },
      ],
    },
    {
      name: 'usaState',
      label: 'State (USA)',
      type: 'select',
      dependsOn: 'country',
      showIf: 'usa',
      options: [
        { label: 'California', value: 'california' },
        { label: 'Texas', value: 'texas' },
        { label: 'Florida', value: 'florida' },
      ],
    },
    {
      name: 'postalCode',
      label: 'Postal Code',
      type: 'text',
      dependsOn: ['state', 'usaState'],
      showIf: (values) => values.state || values.usaState,
      validation: {
        required: true,
        pattern: /^[0-9]{4,8}$/,
        message: 'Postal code must be 4–8 digits',
      },
    },
  ];
8. // Child component that receives a function prop
 
const Button = React.memo(({ onClick, text }) => {

  alert(`Child ${text} button rendered`);

  return <button onClick={onClick}>{text}</button>;

});
 
// Parent component 

function parentComp() {

  const [count1, setCount1] = useState(0);
  const [count2, setCount2] = useState(0);
 
  const handleClick1 = () => {
    setCount1(count1 + 1);
  };
 
  const handleClick2 = useCallback(() => {
    setCount2(count2 + 1);
  }, []);
 
  alert("Parent rendered");

  return (
<div>
<h2>Parent Comp:</h2>
<p>Count 1: {count1}</p>
<p>Count 2: {count2}</p>
<Button onClick={handleClick1} text="Button 1" />
<Button onClick={handleClick2} text="Button 2" />
</div>

  );

}

9. function X(){
  return ("X")
}
function Y(){
  return ("Y")
}
function Z(){
return ("Z")
}



Protiviti

1. // Example Input: "abcabcbb"
// Output: 3

function checkUniqueness(str) {
    let flag = false;
    for (let i = 0; i < str.length; i++) {
        for (let j = i+1; j < str.length; j++) {
            if (str[i] === str[j]) {
                flag = true;
                break;
            }
        }
    }
    if (!flag) {
        return str;
    }
    
    return '';
}

function longestSubstring(str) {
    let i = 0, j = i+1;
    let windowStr = "";
    const hashset = new Set();

    while (i < str.length) {
        windowStr += str[i];
        while (j < str.length) {
            windowStr += str[j];
            checkUniqueness(windowStr)
            hashset.add(checkUniqueness(windowStr));
            j++;
        }
        i++;
        j = i+1;
        windowStr = "";
    }
    
    for (const keys)
    console.log(hashset);
}

longestSubstring("abcabcbb")
2. import React, { useEffect, useState } from 'react';
import './style.css';

export default function App() {
  const [users, setUsers] = useState([]);
  const [search, setSearch] = useState('');

  useEffect(() => {
    fetchUsers();
  }, []);

  const fetchUsers = async () => {
    const response = await fetch('https://jsonplaceholder.typicode.com/users');

    const data = await response.json();

    setUsers(data);
    console.log('data :- ', data)
  };
  
  const handleDelete = (id) => {
    users.splice(
      users.findIndex((user) => user.id === id),
      1
      );
      
    setUsers(users);
  };

  const filteredUsers = users.filter((user) => {
    return user.name.toLowerCase().includes(search.toLowerCase());
  });
  console.log('filteredUsers :- ', filteredUsers, search)

  return (
    <div>
      <h2>User List</h2>

      <input
        type="text"
        value={search}
        onChange={(e) => setSearch(e.target.value)}
        placeholder="Search users..."
      />

      <ul>
        {filteredUsers.map((user, index) => (
          <li key={index}>
            {user.name}

            <button onClick={() => handleDelete(user.id)}>Delete</button>
          </li>
        ))}
      </ul>
    </div>
  );
}





 